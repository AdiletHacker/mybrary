<h2>Recently Added</h2>

<div>
    <% books.forEach(book => { %>
        <img src="<%= book.coverImagePath %>" alt="oops..." width="100" height="150">
        <div><%= book.title %></div>
    <% }) %>
</div>